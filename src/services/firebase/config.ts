// Firebase configuration for SnapClone
// Phase 0: Placeholder configuration - will be replaced with actual Firebase project settings

/**
 * Firebase configuration object.
 * Replace these placeholder values with your actual Firebase project configuration
 * from the Firebase Console when setting up the real backend in Phase 1.
 * 
 * To get your configuration:
 * 1. Go to Firebase Console (https://console.firebase.google.com/)
 * 2. Create a new project or select existing project
 * 3. Add a web app to your project
 * 4. Copy the configuration object and replace the values below
 */
export const firebaseConfig = {
  apiKey: "your-api-key-here",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};

// Firebase app initialization will be added in Phase 1
// import { initializeApp } from 'firebase/app';
// import { getAuth } from 'firebase/auth';
// import { getFirestore } from 'firebase/firestore';
// import { getStorage } from 'firebase/storage';

// export const app = initializeApp(firebaseConfig);
// export const auth = getAuth(app);
// export const db = getFirestore(app);
// export const storage = getStorage(app); 